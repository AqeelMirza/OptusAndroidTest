package com.aqeel.optusandroid.network

import com.aqeel.optusandroid.network.ApiService
import javax.inject.Inject

class ApiHelper @Inject constructor(private val apiService: ApiService) {
  //  suspend fun getVenues(batchGenerator: BatchGenerator) = apiService.getVenues(batchGenerator)
    //suspend fun searchMenu(searchRequest: SearchRequest) = apiService.searchMenu(searchRequest)
}